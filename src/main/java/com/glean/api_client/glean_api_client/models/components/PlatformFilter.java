/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 79535f6079d1
 */
package com.glean.api_client.glean_api_client.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.glean.api_client.glean_api_client.utils.Utils;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.List;
import java.util.Optional;

/**
 * PlatformFilter
 * 
 * <p>A single filter criterion. For `EQUALS`, multiple values within a filter are OR'd. For `NOT_EQUALS`,
 * multiple values exclude all listed values.
 * 
 * <p>Filters are AND'd with each other and with any inline query operators.
 */
public class PlatformFilter {
    /**
     * The field to filter on. Accepts built-in filter field names such as `type`, `owner`, `from`,
     * `author`, `channel`, `status`, `assignee`, `reporter`, `component`, `mentions`, and `collection`,
     * plus custom datasource property names.
     */
    @JsonProperty("field")
    private String field;

    /**
     * One or more values to match.
     */
    @JsonProperty("values")
    private List<String> values;

    /**
     * Comparison operator to apply to this filter. Defaults to `EQUALS`. `GT`, `GTE`, `LT`, and `LTE`
     * range operators require exactly one value; express bounded ranges with multiple filters on the same
     * field.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("operator")
    private Optional<? extends PlatformFilterOperator> operator;

    @JsonCreator
    public PlatformFilter(
            @JsonProperty("field") String field,
            @JsonProperty("values") List<String> values,
            @JsonProperty("operator") Optional<? extends PlatformFilterOperator> operator) {
        Utils.checkNotNull(field, "field");
        Utils.checkNotNull(values, "values");
        Utils.checkNotNull(operator, "operator");
        this.field = field;
        this.values = values;
        this.operator = operator;
    }
    
    public PlatformFilter(
            String field,
            List<String> values) {
        this(field, values, Optional.empty());
    }

    /**
     * The field to filter on. Accepts built-in filter field names such as `type`, `owner`, `from`,
     * `author`, `channel`, `status`, `assignee`, `reporter`, `component`, `mentions`, and `collection`,
     * plus custom datasource property names.
     */
    @JsonIgnore
    public String field() {
        return field;
    }

    /**
     * One or more values to match.
     */
    @JsonIgnore
    public List<String> values() {
        return values;
    }

    /**
     * Comparison operator to apply to this filter. Defaults to `EQUALS`. `GT`, `GTE`, `LT`, and `LTE`
     * range operators require exactly one value; express bounded ranges with multiple filters on the same
     * field.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<PlatformFilterOperator> operator() {
        return (Optional<PlatformFilterOperator>) operator;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * The field to filter on. Accepts built-in filter field names such as `type`, `owner`, `from`,
     * `author`, `channel`, `status`, `assignee`, `reporter`, `component`, `mentions`, and `collection`,
     * plus custom datasource property names.
     */
    public PlatformFilter withField(String field) {
        Utils.checkNotNull(field, "field");
        this.field = field;
        return this;
    }

    /**
     * One or more values to match.
     */
    public PlatformFilter withValues(List<String> values) {
        Utils.checkNotNull(values, "values");
        this.values = values;
        return this;
    }

    /**
     * Comparison operator to apply to this filter. Defaults to `EQUALS`. `GT`, `GTE`, `LT`, and `LTE`
     * range operators require exactly one value; express bounded ranges with multiple filters on the same
     * field.
     */
    public PlatformFilter withOperator(PlatformFilterOperator operator) {
        Utils.checkNotNull(operator, "operator");
        this.operator = Optional.ofNullable(operator);
        return this;
    }


    /**
     * Comparison operator to apply to this filter. Defaults to `EQUALS`. `GT`, `GTE`, `LT`, and `LTE`
     * range operators require exactly one value; express bounded ranges with multiple filters on the same
     * field.
     */
    public PlatformFilter withOperator(Optional<? extends PlatformFilterOperator> operator) {
        Utils.checkNotNull(operator, "operator");
        this.operator = operator;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        PlatformFilter other = (PlatformFilter) o;
        return 
            Utils.enhancedDeepEquals(this.field, other.field) &&
            Utils.enhancedDeepEquals(this.values, other.values) &&
            Utils.enhancedDeepEquals(this.operator, other.operator);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            field, values, operator);
    }
    
    @Override
    public String toString() {
        return Utils.toString(PlatformFilter.class,
                "field", field,
                "values", values,
                "operator", operator);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String field;

        private List<String> values;

        private Optional<? extends PlatformFilterOperator> operator = Optional.empty();

        private Builder() {
          // force use of static builder() method
        }


        /**
         * The field to filter on. Accepts built-in filter field names such as `type`, `owner`, `from`,
         * `author`, `channel`, `status`, `assignee`, `reporter`, `component`, `mentions`, and `collection`,
         * plus custom datasource property names.
         */
        public Builder field(String field) {
            Utils.checkNotNull(field, "field");
            this.field = field;
            return this;
        }


        /**
         * One or more values to match.
         */
        public Builder values(List<String> values) {
            Utils.checkNotNull(values, "values");
            this.values = values;
            return this;
        }


        /**
         * Comparison operator to apply to this filter. Defaults to `EQUALS`. `GT`, `GTE`, `LT`, and `LTE`
         * range operators require exactly one value; express bounded ranges with multiple filters on the same
         * field.
         */
        public Builder operator(PlatformFilterOperator operator) {
            Utils.checkNotNull(operator, "operator");
            this.operator = Optional.ofNullable(operator);
            return this;
        }

        /**
         * Comparison operator to apply to this filter. Defaults to `EQUALS`. `GT`, `GTE`, `LT`, and `LTE`
         * range operators require exactly one value; express bounded ranges with multiple filters on the same
         * field.
         */
        public Builder operator(Optional<? extends PlatformFilterOperator> operator) {
            Utils.checkNotNull(operator, "operator");
            this.operator = operator;
            return this;
        }

        public PlatformFilter build() {

            return new PlatformFilter(
                field, values, operator);
        }

    }
}
