/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 908792f27bb8
 */
package com.glean.api_client.glean_api_client.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.glean.api_client.glean_api_client.utils.Utils;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.Optional;


public class PlatformTriggerDeliveryOutput {
    /**
     * HTTPS URL that signed events are delivered to.
     */
    @JsonProperty("webhook_url")
    private String webhookUrl;

    /**
     * Optional caller credential sent as an HTTP auth header on each delivery, in addition to the HMAC
     * signature. Lets the receiving endpoint authenticate the request. On update, omit `auth` to preserve
     * the existing credential; there is no in-place removal — recreate the trigger to remove auth.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("auth")
    private Optional<? extends PlatformTriggerAuthOutput> auth;

    @JsonCreator
    public PlatformTriggerDeliveryOutput(
            @JsonProperty("webhook_url") String webhookUrl,
            @JsonProperty("auth") Optional<? extends PlatformTriggerAuthOutput> auth) {
        Utils.checkNotNull(webhookUrl, "webhookUrl");
        Utils.checkNotNull(auth, "auth");
        this.webhookUrl = webhookUrl;
        this.auth = auth;
    }
    
    public PlatformTriggerDeliveryOutput(
            String webhookUrl) {
        this(webhookUrl, Optional.empty());
    }

    /**
     * HTTPS URL that signed events are delivered to.
     */
    @JsonIgnore
    public String webhookUrl() {
        return webhookUrl;
    }

    /**
     * Optional caller credential sent as an HTTP auth header on each delivery, in addition to the HMAC
     * signature. Lets the receiving endpoint authenticate the request. On update, omit `auth` to preserve
     * the existing credential; there is no in-place removal — recreate the trigger to remove auth.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<PlatformTriggerAuthOutput> auth() {
        return (Optional<PlatformTriggerAuthOutput>) auth;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * HTTPS URL that signed events are delivered to.
     */
    public PlatformTriggerDeliveryOutput withWebhookUrl(String webhookUrl) {
        Utils.checkNotNull(webhookUrl, "webhookUrl");
        this.webhookUrl = webhookUrl;
        return this;
    }

    /**
     * Optional caller credential sent as an HTTP auth header on each delivery, in addition to the HMAC
     * signature. Lets the receiving endpoint authenticate the request. On update, omit `auth` to preserve
     * the existing credential; there is no in-place removal — recreate the trigger to remove auth.
     */
    public PlatformTriggerDeliveryOutput withAuth(PlatformTriggerAuthOutput auth) {
        Utils.checkNotNull(auth, "auth");
        this.auth = Optional.ofNullable(auth);
        return this;
    }


    /**
     * Optional caller credential sent as an HTTP auth header on each delivery, in addition to the HMAC
     * signature. Lets the receiving endpoint authenticate the request. On update, omit `auth` to preserve
     * the existing credential; there is no in-place removal — recreate the trigger to remove auth.
     */
    public PlatformTriggerDeliveryOutput withAuth(Optional<? extends PlatformTriggerAuthOutput> auth) {
        Utils.checkNotNull(auth, "auth");
        this.auth = auth;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        PlatformTriggerDeliveryOutput other = (PlatformTriggerDeliveryOutput) o;
        return 
            Utils.enhancedDeepEquals(this.webhookUrl, other.webhookUrl) &&
            Utils.enhancedDeepEquals(this.auth, other.auth);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            webhookUrl, auth);
    }
    
    @Override
    public String toString() {
        return Utils.toString(PlatformTriggerDeliveryOutput.class,
                "webhookUrl", webhookUrl,
                "auth", auth);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String webhookUrl;

        private Optional<? extends PlatformTriggerAuthOutput> auth = Optional.empty();

        private Builder() {
          // force use of static builder() method
        }


        /**
         * HTTPS URL that signed events are delivered to.
         */
        public Builder webhookUrl(String webhookUrl) {
            Utils.checkNotNull(webhookUrl, "webhookUrl");
            this.webhookUrl = webhookUrl;
            return this;
        }


        /**
         * Optional caller credential sent as an HTTP auth header on each delivery, in addition to the HMAC
         * signature. Lets the receiving endpoint authenticate the request. On update, omit `auth` to preserve
         * the existing credential; there is no in-place removal — recreate the trigger to remove auth.
         */
        public Builder auth(PlatformTriggerAuthOutput auth) {
            Utils.checkNotNull(auth, "auth");
            this.auth = Optional.ofNullable(auth);
            return this;
        }

        /**
         * Optional caller credential sent as an HTTP auth header on each delivery, in addition to the HMAC
         * signature. Lets the receiving endpoint authenticate the request. On update, omit `auth` to preserve
         * the existing credential; there is no in-place removal — recreate the trigger to remove auth.
         */
        public Builder auth(Optional<? extends PlatformTriggerAuthOutput> auth) {
            Utils.checkNotNull(auth, "auth");
            this.auth = auth;
            return this;
        }

        public PlatformTriggerDeliveryOutput build() {

            return new PlatformTriggerDeliveryOutput(
                webhookUrl, auth);
        }

    }
}
