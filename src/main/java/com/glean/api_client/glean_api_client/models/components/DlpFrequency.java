/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 28cba0614d04
 */
package com.glean.api_client.glean_api_client.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * DlpFrequency
 * 
 * <p>Interval between scans. DAILY is deprecated.
 */
public class DlpFrequency {

    public static final DlpFrequency ONCE = new DlpFrequency("ONCE");
    public static final DlpFrequency DAILY = new DlpFrequency("DAILY");
    public static final DlpFrequency WEEKLY = new DlpFrequency("WEEKLY");
    public static final DlpFrequency CONTINUOUS = new DlpFrequency("CONTINUOUS");
    public static final DlpFrequency NONE = new DlpFrequency("NONE");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, DlpFrequency> values = createValuesMap();
    private static final Map<String, DlpFrequencyEnum> enums = createEnumsMap();

    private final String value;

    private DlpFrequency(String value) {
        this.value = value;
    }

    /**
     * Returns a DlpFrequency with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as DlpFrequency
     */ 
    @JsonCreator
    public static DlpFrequency of(String value) {
        synchronized (DlpFrequency.class) {
            return values.computeIfAbsent(value, v -> new DlpFrequency(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<DlpFrequencyEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        DlpFrequency other = (DlpFrequency) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "DlpFrequency [value=" + value + "]";
    }

    // return an array just like an enum
    public static DlpFrequency[] values() {
        synchronized (DlpFrequency.class) {
            return values.values().toArray(new DlpFrequency[] {});
        }
    }

    private static final Map<String, DlpFrequency> createValuesMap() {
        Map<String, DlpFrequency> map = new LinkedHashMap<>();
        map.put("ONCE", ONCE);
        map.put("DAILY", DAILY);
        map.put("WEEKLY", WEEKLY);
        map.put("CONTINUOUS", CONTINUOUS);
        map.put("NONE", NONE);
        return map;
    }

    private static final Map<String, DlpFrequencyEnum> createEnumsMap() {
        Map<String, DlpFrequencyEnum> map = new HashMap<>();
        map.put("ONCE", DlpFrequencyEnum.ONCE);
        map.put("DAILY", DlpFrequencyEnum.DAILY);
        map.put("WEEKLY", DlpFrequencyEnum.WEEKLY);
        map.put("CONTINUOUS", DlpFrequencyEnum.CONTINUOUS);
        map.put("NONE", DlpFrequencyEnum.NONE);
        return map;
    }
    
    
    public enum DlpFrequencyEnum {

        ONCE("ONCE"),
        DAILY("DAILY"),
        WEEKLY("WEEKLY"),
        CONTINUOUS("CONTINUOUS"),
        NONE("NONE"),;

        private final String value;

        private DlpFrequencyEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

