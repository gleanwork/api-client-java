/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 666f97e5824a
 */
package com.glean.api_client.glean_api_client.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.glean.api_client.glean_api_client.utils.Utils;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.List;
import java.util.Optional;


public class PlatformFilterFieldInfo {
    /**
     * Field name usable in the `filters` array for `POST /api/search`.
     */
    @JsonProperty("field")
    private String field;

    /**
     * Open field-type string. Current values are `STRING` for text and keyword fields and `USER` for
     * people fields (`owner`, `from`, `author`, `assignee`, `reporter`, `mentions`). Clients must tolerate
     * unknown values.
     */
    @JsonProperty("type")
    private String type;

    /**
     * Comparison operators supported for this field. Built-in fields expose `EQUALS` and `NOT_EQUALS`.
     */
    @JsonProperty("operators")
    private List<PlatformFilterOperator> operators;

    /**
     * Suggested filter values observed for the supplied `query`. Omitted in catalog-only responses.
     * Present as an array, including `[]`, when `query` is provided.
     * 
     * <p>Values are a bounded, non-exhaustive sample in search-provider order and may include duplicates;
     * blank values are omitted. No additional endpoint-specific truncation is applied. Grouping in the
     * search UI is not preserved; each value applies to the field broadly.
     * 
     * <p>An empty array does not mean no valid value exists. Availability varies by data source; federated
     * and MCP integrations may return page-derived values or none at all. Reusing a suggested value in
     * `POST /api/search` does not guarantee matching results or identical filter behavior.
     * 
     * <p>For `USER` fields, values may be email addresses or display names.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("values")
    private Optional<? extends List<String>> values;

    @JsonCreator
    public PlatformFilterFieldInfo(
            @JsonProperty("field") String field,
            @JsonProperty("type") String type,
            @JsonProperty("operators") List<PlatformFilterOperator> operators,
            @JsonProperty("values") Optional<? extends List<String>> values) {
        Utils.checkNotNull(field, "field");
        Utils.checkNotNull(type, "type");
        Utils.checkNotNull(operators, "operators");
        Utils.checkNotNull(values, "values");
        this.field = field;
        this.type = type;
        this.operators = operators;
        this.values = values;
    }
    
    public PlatformFilterFieldInfo(
            String field,
            String type,
            List<PlatformFilterOperator> operators) {
        this(field, type, operators,
            Optional.empty());
    }

    /**
     * Field name usable in the `filters` array for `POST /api/search`.
     */
    @JsonIgnore
    public String field() {
        return field;
    }

    /**
     * Open field-type string. Current values are `STRING` for text and keyword fields and `USER` for
     * people fields (`owner`, `from`, `author`, `assignee`, `reporter`, `mentions`). Clients must tolerate
     * unknown values.
     */
    @JsonIgnore
    public String type() {
        return type;
    }

    /**
     * Comparison operators supported for this field. Built-in fields expose `EQUALS` and `NOT_EQUALS`.
     */
    @JsonIgnore
    public List<PlatformFilterOperator> operators() {
        return operators;
    }

    /**
     * Suggested filter values observed for the supplied `query`. Omitted in catalog-only responses.
     * Present as an array, including `[]`, when `query` is provided.
     * 
     * <p>Values are a bounded, non-exhaustive sample in search-provider order and may include duplicates;
     * blank values are omitted. No additional endpoint-specific truncation is applied. Grouping in the
     * search UI is not preserved; each value applies to the field broadly.
     * 
     * <p>An empty array does not mean no valid value exists. Availability varies by data source; federated
     * and MCP integrations may return page-derived values or none at all. Reusing a suggested value in
     * `POST /api/search` does not guarantee matching results or identical filter behavior.
     * 
     * <p>For `USER` fields, values may be email addresses or display names.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<List<String>> values() {
        return (Optional<List<String>>) values;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * Field name usable in the `filters` array for `POST /api/search`.
     */
    public PlatformFilterFieldInfo withField(String field) {
        Utils.checkNotNull(field, "field");
        this.field = field;
        return this;
    }

    /**
     * Open field-type string. Current values are `STRING` for text and keyword fields and `USER` for
     * people fields (`owner`, `from`, `author`, `assignee`, `reporter`, `mentions`). Clients must tolerate
     * unknown values.
     */
    public PlatformFilterFieldInfo withType(String type) {
        Utils.checkNotNull(type, "type");
        this.type = type;
        return this;
    }

    /**
     * Comparison operators supported for this field. Built-in fields expose `EQUALS` and `NOT_EQUALS`.
     */
    public PlatformFilterFieldInfo withOperators(List<PlatformFilterOperator> operators) {
        Utils.checkNotNull(operators, "operators");
        this.operators = operators;
        return this;
    }

    /**
     * Suggested filter values observed for the supplied `query`. Omitted in catalog-only responses.
     * Present as an array, including `[]`, when `query` is provided.
     * 
     * <p>Values are a bounded, non-exhaustive sample in search-provider order and may include duplicates;
     * blank values are omitted. No additional endpoint-specific truncation is applied. Grouping in the
     * search UI is not preserved; each value applies to the field broadly.
     * 
     * <p>An empty array does not mean no valid value exists. Availability varies by data source; federated
     * and MCP integrations may return page-derived values or none at all. Reusing a suggested value in
     * `POST /api/search` does not guarantee matching results or identical filter behavior.
     * 
     * <p>For `USER` fields, values may be email addresses or display names.
     */
    public PlatformFilterFieldInfo withValues(List<String> values) {
        Utils.checkNotNull(values, "values");
        this.values = Optional.ofNullable(values);
        return this;
    }


    /**
     * Suggested filter values observed for the supplied `query`. Omitted in catalog-only responses.
     * Present as an array, including `[]`, when `query` is provided.
     * 
     * <p>Values are a bounded, non-exhaustive sample in search-provider order and may include duplicates;
     * blank values are omitted. No additional endpoint-specific truncation is applied. Grouping in the
     * search UI is not preserved; each value applies to the field broadly.
     * 
     * <p>An empty array does not mean no valid value exists. Availability varies by data source; federated
     * and MCP integrations may return page-derived values or none at all. Reusing a suggested value in
     * `POST /api/search` does not guarantee matching results or identical filter behavior.
     * 
     * <p>For `USER` fields, values may be email addresses or display names.
     */
    public PlatformFilterFieldInfo withValues(Optional<? extends List<String>> values) {
        Utils.checkNotNull(values, "values");
        this.values = values;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        PlatformFilterFieldInfo other = (PlatformFilterFieldInfo) o;
        return 
            Utils.enhancedDeepEquals(this.field, other.field) &&
            Utils.enhancedDeepEquals(this.type, other.type) &&
            Utils.enhancedDeepEquals(this.operators, other.operators) &&
            Utils.enhancedDeepEquals(this.values, other.values);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            field, type, operators,
            values);
    }
    
    @Override
    public String toString() {
        return Utils.toString(PlatformFilterFieldInfo.class,
                "field", field,
                "type", type,
                "operators", operators,
                "values", values);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String field;

        private String type;

        private List<PlatformFilterOperator> operators;

        private Optional<? extends List<String>> values = Optional.empty();

        private Builder() {
          // force use of static builder() method
        }


        /**
         * Field name usable in the `filters` array for `POST /api/search`.
         */
        public Builder field(String field) {
            Utils.checkNotNull(field, "field");
            this.field = field;
            return this;
        }


        /**
         * Open field-type string. Current values are `STRING` for text and keyword fields and `USER` for
         * people fields (`owner`, `from`, `author`, `assignee`, `reporter`, `mentions`). Clients must tolerate
         * unknown values.
         */
        public Builder type(String type) {
            Utils.checkNotNull(type, "type");
            this.type = type;
            return this;
        }


        /**
         * Comparison operators supported for this field. Built-in fields expose `EQUALS` and `NOT_EQUALS`.
         */
        public Builder operators(List<PlatformFilterOperator> operators) {
            Utils.checkNotNull(operators, "operators");
            this.operators = operators;
            return this;
        }


        /**
         * Suggested filter values observed for the supplied `query`. Omitted in catalog-only responses.
         * Present as an array, including `[]`, when `query` is provided.
         * 
         * <p>Values are a bounded, non-exhaustive sample in search-provider order and may include duplicates;
         * blank values are omitted. No additional endpoint-specific truncation is applied. Grouping in the
         * search UI is not preserved; each value applies to the field broadly.
         * 
         * <p>An empty array does not mean no valid value exists. Availability varies by data source; federated
         * and MCP integrations may return page-derived values or none at all. Reusing a suggested value in
         * `POST /api/search` does not guarantee matching results or identical filter behavior.
         * 
         * <p>For `USER` fields, values may be email addresses or display names.
         */
        public Builder values(List<String> values) {
            Utils.checkNotNull(values, "values");
            this.values = Optional.ofNullable(values);
            return this;
        }

        /**
         * Suggested filter values observed for the supplied `query`. Omitted in catalog-only responses.
         * Present as an array, including `[]`, when `query` is provided.
         * 
         * <p>Values are a bounded, non-exhaustive sample in search-provider order and may include duplicates;
         * blank values are omitted. No additional endpoint-specific truncation is applied. Grouping in the
         * search UI is not preserved; each value applies to the field broadly.
         * 
         * <p>An empty array does not mean no valid value exists. Availability varies by data source; federated
         * and MCP integrations may return page-derived values or none at all. Reusing a suggested value in
         * `POST /api/search` does not guarantee matching results or identical filter behavior.
         * 
         * <p>For `USER` fields, values may be email addresses or display names.
         */
        public Builder values(Optional<? extends List<String>> values) {
            Utils.checkNotNull(values, "values");
            this.values = values;
            return this;
        }

        public PlatformFilterFieldInfo build() {

            return new PlatformFilterFieldInfo(
                field, type, operators,
                values);
        }

    }
}
