/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 32f457fb3009
 */
package com.glean.api_client.glean_api_client.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * AuthStatus
 * 
 * <p>The per-user authorization status for a datasource.
 */
public class AuthStatus {

    public static final AuthStatus DISABLED = new AuthStatus("DISABLED");
    public static final AuthStatus AWAITING_AUTH = new AuthStatus("AWAITING_AUTH");
    public static final AuthStatus AUTHORIZED = new AuthStatus("AUTHORIZED");
    public static final AuthStatus STALE_OAUTH = new AuthStatus("STALE_OAUTH");
    public static final AuthStatus SEG_MIGRATION = new AuthStatus("SEG_MIGRATION");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, AuthStatus> values = createValuesMap();
    private static final Map<String, AuthStatusEnum> enums = createEnumsMap();

    private final String value;

    private AuthStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a AuthStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as AuthStatus
     */ 
    @JsonCreator
    public static AuthStatus of(String value) {
        synchronized (AuthStatus.class) {
            return values.computeIfAbsent(value, v -> new AuthStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<AuthStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        AuthStatus other = (AuthStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "AuthStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static AuthStatus[] values() {
        synchronized (AuthStatus.class) {
            return values.values().toArray(new AuthStatus[] {});
        }
    }

    private static final Map<String, AuthStatus> createValuesMap() {
        Map<String, AuthStatus> map = new LinkedHashMap<>();
        map.put("DISABLED", DISABLED);
        map.put("AWAITING_AUTH", AWAITING_AUTH);
        map.put("AUTHORIZED", AUTHORIZED);
        map.put("STALE_OAUTH", STALE_OAUTH);
        map.put("SEG_MIGRATION", SEG_MIGRATION);
        return map;
    }

    private static final Map<String, AuthStatusEnum> createEnumsMap() {
        Map<String, AuthStatusEnum> map = new HashMap<>();
        map.put("DISABLED", AuthStatusEnum.DISABLED);
        map.put("AWAITING_AUTH", AuthStatusEnum.AWAITING_AUTH);
        map.put("AUTHORIZED", AuthStatusEnum.AUTHORIZED);
        map.put("STALE_OAUTH", AuthStatusEnum.STALE_OAUTH);
        map.put("SEG_MIGRATION", AuthStatusEnum.SEG_MIGRATION);
        return map;
    }
    
    
    public enum AuthStatusEnum {

        DISABLED("DISABLED"),
        AWAITING_AUTH("AWAITING_AUTH"),
        AUTHORIZED("AUTHORIZED"),
        STALE_OAUTH("STALE_OAUTH"),
        SEG_MIGRATION("SEG_MIGRATION"),;

        private final String value;

        private AuthStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

