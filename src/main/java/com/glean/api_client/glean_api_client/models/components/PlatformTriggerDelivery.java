/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 0f636bf5090f
 */
package com.glean.api_client.glean_api_client.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.glean.api_client.glean_api_client.utils.Utils;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.Optional;


public class PlatformTriggerDelivery {
    /**
     * HTTPS URL that signed events are delivered to.
     */
    @JsonProperty("webhook_url")
    private String webhookUrl;

    /**
     * Optional caller credential sent as an HTTP auth header on each delivery, in addition to the HMAC
     * signature. Lets the receiving endpoint authenticate the request. On update, omit `auth` to preserve
     * the existing credential; there is no in-place removal — recreate the trigger to remove auth.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("auth")
    private Optional<? extends PlatformTriggerAuth> auth;

    @JsonCreator
    public PlatformTriggerDelivery(
            @JsonProperty("webhook_url") String webhookUrl,
            @JsonProperty("auth") Optional<? extends PlatformTriggerAuth> auth) {
        Utils.checkNotNull(webhookUrl, "webhookUrl");
        Utils.checkNotNull(auth, "auth");
        this.webhookUrl = webhookUrl;
        this.auth = auth;
    }
    
    public PlatformTriggerDelivery(
            String webhookUrl) {
        this(webhookUrl, Optional.empty());
    }

    /**
     * HTTPS URL that signed events are delivered to.
     */
    @JsonIgnore
    public String webhookUrl() {
        return webhookUrl;
    }

    /**
     * Optional caller credential sent as an HTTP auth header on each delivery, in addition to the HMAC
     * signature. Lets the receiving endpoint authenticate the request. On update, omit `auth` to preserve
     * the existing credential; there is no in-place removal — recreate the trigger to remove auth.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<PlatformTriggerAuth> auth() {
        return (Optional<PlatformTriggerAuth>) auth;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * HTTPS URL that signed events are delivered to.
     */
    public PlatformTriggerDelivery withWebhookUrl(String webhookUrl) {
        Utils.checkNotNull(webhookUrl, "webhookUrl");
        this.webhookUrl = webhookUrl;
        return this;
    }

    /**
     * Optional caller credential sent as an HTTP auth header on each delivery, in addition to the HMAC
     * signature. Lets the receiving endpoint authenticate the request. On update, omit `auth` to preserve
     * the existing credential; there is no in-place removal — recreate the trigger to remove auth.
     */
    public PlatformTriggerDelivery withAuth(PlatformTriggerAuth auth) {
        Utils.checkNotNull(auth, "auth");
        this.auth = Optional.ofNullable(auth);
        return this;
    }


    /**
     * Optional caller credential sent as an HTTP auth header on each delivery, in addition to the HMAC
     * signature. Lets the receiving endpoint authenticate the request. On update, omit `auth` to preserve
     * the existing credential; there is no in-place removal — recreate the trigger to remove auth.
     */
    public PlatformTriggerDelivery withAuth(Optional<? extends PlatformTriggerAuth> auth) {
        Utils.checkNotNull(auth, "auth");
        this.auth = auth;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        PlatformTriggerDelivery other = (PlatformTriggerDelivery) o;
        return 
            Utils.enhancedDeepEquals(this.webhookUrl, other.webhookUrl) &&
            Utils.enhancedDeepEquals(this.auth, other.auth);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            webhookUrl, auth);
    }
    
    @Override
    public String toString() {
        return Utils.toString(PlatformTriggerDelivery.class,
                "webhookUrl", webhookUrl,
                "auth", auth);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String webhookUrl;

        private Optional<? extends PlatformTriggerAuth> auth = Optional.empty();

        private Builder() {
          // force use of static builder() method
        }


        /**
         * HTTPS URL that signed events are delivered to.
         */
        public Builder webhookUrl(String webhookUrl) {
            Utils.checkNotNull(webhookUrl, "webhookUrl");
            this.webhookUrl = webhookUrl;
            return this;
        }


        /**
         * Optional caller credential sent as an HTTP auth header on each delivery, in addition to the HMAC
         * signature. Lets the receiving endpoint authenticate the request. On update, omit `auth` to preserve
         * the existing credential; there is no in-place removal — recreate the trigger to remove auth.
         */
        public Builder auth(PlatformTriggerAuth auth) {
            Utils.checkNotNull(auth, "auth");
            this.auth = Optional.ofNullable(auth);
            return this;
        }

        /**
         * Optional caller credential sent as an HTTP auth header on each delivery, in addition to the HMAC
         * signature. Lets the receiving endpoint authenticate the request. On update, omit `auth` to preserve
         * the existing credential; there is no in-place removal — recreate the trigger to remove auth.
         */
        public Builder auth(Optional<? extends PlatformTriggerAuth> auth) {
            Utils.checkNotNull(auth, "auth");
            this.auth = auth;
            return this;
        }

        public PlatformTriggerDelivery build() {

            return new PlatformTriggerDelivery(
                webhookUrl, auth);
        }

    }
}
