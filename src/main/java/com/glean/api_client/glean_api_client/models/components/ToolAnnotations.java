/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 25cd92cd7191
 */
package com.glean.api_client.glean_api_client.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.glean.api_client.glean_api_client.utils.Utils;
import java.lang.Boolean;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;
import org.openapitools.jackson.nullable.JsonNullable;

/**
 * ToolAnnotations
 * 
 * <p>Behavioural hints for the tool, exactly as declared by the tool server. No
 * defaults are filled in — see the per-field notes for what an absent value means.
 * 
 * <p>Per the MCP specification, clients MUST treat these hints as untrusted unless
 * the tool server is itself trusted. They are advisory metadata, not an
 * authorization signal, and must not be the sole basis of a security decision.
 */
public class ToolAnnotations {

    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("title")
    private Optional<String> title;

    /**
     * True if the tool does not modify its environment. Always present. MCP
     * defines no distinction between an undeclared value and `false` here, so
     * `false` means "not declared read-only" rather than "declared mutating".
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("readOnlyHint")
    private Optional<Boolean> readOnlyHint;

    /**
     * True if the tool may perform destructive updates. **Absent means the server
     * did not declare it**, for which the MCP default is `true`. Only meaningful
     * when `readOnlyHint` is `false`.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("destructiveHint")
    private JsonNullable<Boolean> destructiveHint;

    /**
     * True if repeated calls with the same arguments have no additional effect.
     * Always present; as with `readOnlyHint`, `false` means "not declared
     * idempotent". Only meaningful when `readOnlyHint` is `false`.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("idempotentHint")
    private Optional<Boolean> idempotentHint;

    /**
     * True if the tool interacts with an open world of external entities. **Absent
     * means the server did not declare it**, for which the MCP default is `true`.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("openWorldHint")
    private JsonNullable<Boolean> openWorldHint;

    @JsonCreator
    public ToolAnnotations(
            @JsonProperty("title") Optional<String> title,
            @JsonProperty("readOnlyHint") Optional<Boolean> readOnlyHint,
            @JsonProperty("destructiveHint") JsonNullable<Boolean> destructiveHint,
            @JsonProperty("idempotentHint") Optional<Boolean> idempotentHint,
            @JsonProperty("openWorldHint") JsonNullable<Boolean> openWorldHint) {
        Utils.checkNotNull(title, "title");
        Utils.checkNotNull(readOnlyHint, "readOnlyHint");
        Utils.checkNotNull(destructiveHint, "destructiveHint");
        Utils.checkNotNull(idempotentHint, "idempotentHint");
        Utils.checkNotNull(openWorldHint, "openWorldHint");
        this.title = title;
        this.readOnlyHint = readOnlyHint;
        this.destructiveHint = destructiveHint;
        this.idempotentHint = idempotentHint;
        this.openWorldHint = openWorldHint;
    }
    
    public ToolAnnotations() {
        this(Optional.empty(), Optional.empty(), JsonNullable.undefined(),
            Optional.empty(), JsonNullable.undefined());
    }

    @JsonIgnore
    public Optional<String> title() {
        return title;
    }

    /**
     * True if the tool does not modify its environment. Always present. MCP
     * defines no distinction between an undeclared value and `false` here, so
     * `false` means "not declared read-only" rather than "declared mutating".
     */
    @JsonIgnore
    public Optional<Boolean> readOnlyHint() {
        return readOnlyHint;
    }

    /**
     * True if the tool may perform destructive updates. **Absent means the server
     * did not declare it**, for which the MCP default is `true`. Only meaningful
     * when `readOnlyHint` is `false`.
     */
    @JsonIgnore
    public JsonNullable<Boolean> destructiveHint() {
        return destructiveHint;
    }

    /**
     * True if repeated calls with the same arguments have no additional effect.
     * Always present; as with `readOnlyHint`, `false` means "not declared
     * idempotent". Only meaningful when `readOnlyHint` is `false`.
     */
    @JsonIgnore
    public Optional<Boolean> idempotentHint() {
        return idempotentHint;
    }

    /**
     * True if the tool interacts with an open world of external entities. **Absent
     * means the server did not declare it**, for which the MCP default is `true`.
     */
    @JsonIgnore
    public JsonNullable<Boolean> openWorldHint() {
        return openWorldHint;
    }

    public static Builder builder() {
        return new Builder();
    }


    public ToolAnnotations withTitle(String title) {
        Utils.checkNotNull(title, "title");
        this.title = Optional.ofNullable(title);
        return this;
    }


    public ToolAnnotations withTitle(Optional<String> title) {
        Utils.checkNotNull(title, "title");
        this.title = title;
        return this;
    }

    /**
     * True if the tool does not modify its environment. Always present. MCP
     * defines no distinction between an undeclared value and `false` here, so
     * `false` means "not declared read-only" rather than "declared mutating".
     */
    public ToolAnnotations withReadOnlyHint(boolean readOnlyHint) {
        Utils.checkNotNull(readOnlyHint, "readOnlyHint");
        this.readOnlyHint = Optional.ofNullable(readOnlyHint);
        return this;
    }


    /**
     * True if the tool does not modify its environment. Always present. MCP
     * defines no distinction between an undeclared value and `false` here, so
     * `false` means "not declared read-only" rather than "declared mutating".
     */
    public ToolAnnotations withReadOnlyHint(Optional<Boolean> readOnlyHint) {
        Utils.checkNotNull(readOnlyHint, "readOnlyHint");
        this.readOnlyHint = readOnlyHint;
        return this;
    }

    /**
     * True if the tool may perform destructive updates. **Absent means the server
     * did not declare it**, for which the MCP default is `true`. Only meaningful
     * when `readOnlyHint` is `false`.
     */
    public ToolAnnotations withDestructiveHint(boolean destructiveHint) {
        Utils.checkNotNull(destructiveHint, "destructiveHint");
        this.destructiveHint = JsonNullable.of(destructiveHint);
        return this;
    }

    /**
     * True if the tool may perform destructive updates. **Absent means the server
     * did not declare it**, for which the MCP default is `true`. Only meaningful
     * when `readOnlyHint` is `false`.
     */
    public ToolAnnotations withDestructiveHint(JsonNullable<Boolean> destructiveHint) {
        Utils.checkNotNull(destructiveHint, "destructiveHint");
        this.destructiveHint = destructiveHint;
        return this;
    }

    /**
     * True if repeated calls with the same arguments have no additional effect.
     * Always present; as with `readOnlyHint`, `false` means "not declared
     * idempotent". Only meaningful when `readOnlyHint` is `false`.
     */
    public ToolAnnotations withIdempotentHint(boolean idempotentHint) {
        Utils.checkNotNull(idempotentHint, "idempotentHint");
        this.idempotentHint = Optional.ofNullable(idempotentHint);
        return this;
    }


    /**
     * True if repeated calls with the same arguments have no additional effect.
     * Always present; as with `readOnlyHint`, `false` means "not declared
     * idempotent". Only meaningful when `readOnlyHint` is `false`.
     */
    public ToolAnnotations withIdempotentHint(Optional<Boolean> idempotentHint) {
        Utils.checkNotNull(idempotentHint, "idempotentHint");
        this.idempotentHint = idempotentHint;
        return this;
    }

    /**
     * True if the tool interacts with an open world of external entities. **Absent
     * means the server did not declare it**, for which the MCP default is `true`.
     */
    public ToolAnnotations withOpenWorldHint(boolean openWorldHint) {
        Utils.checkNotNull(openWorldHint, "openWorldHint");
        this.openWorldHint = JsonNullable.of(openWorldHint);
        return this;
    }

    /**
     * True if the tool interacts with an open world of external entities. **Absent
     * means the server did not declare it**, for which the MCP default is `true`.
     */
    public ToolAnnotations withOpenWorldHint(JsonNullable<Boolean> openWorldHint) {
        Utils.checkNotNull(openWorldHint, "openWorldHint");
        this.openWorldHint = openWorldHint;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ToolAnnotations other = (ToolAnnotations) o;
        return 
            Utils.enhancedDeepEquals(this.title, other.title) &&
            Utils.enhancedDeepEquals(this.readOnlyHint, other.readOnlyHint) &&
            Utils.enhancedDeepEquals(this.destructiveHint, other.destructiveHint) &&
            Utils.enhancedDeepEquals(this.idempotentHint, other.idempotentHint) &&
            Utils.enhancedDeepEquals(this.openWorldHint, other.openWorldHint);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            title, readOnlyHint, destructiveHint,
            idempotentHint, openWorldHint);
    }
    
    @Override
    public String toString() {
        return Utils.toString(ToolAnnotations.class,
                "title", title,
                "readOnlyHint", readOnlyHint,
                "destructiveHint", destructiveHint,
                "idempotentHint", idempotentHint,
                "openWorldHint", openWorldHint);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private Optional<String> title = Optional.empty();

        private Optional<Boolean> readOnlyHint = Optional.empty();

        private JsonNullable<Boolean> destructiveHint = JsonNullable.undefined();

        private Optional<Boolean> idempotentHint = Optional.empty();

        private JsonNullable<Boolean> openWorldHint = JsonNullable.undefined();

        private Builder() {
          // force use of static builder() method
        }


        public Builder title(String title) {
            Utils.checkNotNull(title, "title");
            this.title = Optional.ofNullable(title);
            return this;
        }

        public Builder title(Optional<String> title) {
            Utils.checkNotNull(title, "title");
            this.title = title;
            return this;
        }


        /**
         * True if the tool does not modify its environment. Always present. MCP
         * defines no distinction between an undeclared value and `false` here, so
         * `false` means "not declared read-only" rather than "declared mutating".
         */
        public Builder readOnlyHint(boolean readOnlyHint) {
            Utils.checkNotNull(readOnlyHint, "readOnlyHint");
            this.readOnlyHint = Optional.ofNullable(readOnlyHint);
            return this;
        }

        /**
         * True if the tool does not modify its environment. Always present. MCP
         * defines no distinction between an undeclared value and `false` here, so
         * `false` means "not declared read-only" rather than "declared mutating".
         */
        public Builder readOnlyHint(Optional<Boolean> readOnlyHint) {
            Utils.checkNotNull(readOnlyHint, "readOnlyHint");
            this.readOnlyHint = readOnlyHint;
            return this;
        }


        /**
         * True if the tool may perform destructive updates. **Absent means the server
         * did not declare it**, for which the MCP default is `true`. Only meaningful
         * when `readOnlyHint` is `false`.
         */
        public Builder destructiveHint(boolean destructiveHint) {
            Utils.checkNotNull(destructiveHint, "destructiveHint");
            this.destructiveHint = JsonNullable.of(destructiveHint);
            return this;
        }

        /**
         * True if the tool may perform destructive updates. **Absent means the server
         * did not declare it**, for which the MCP default is `true`. Only meaningful
         * when `readOnlyHint` is `false`.
         */
        public Builder destructiveHint(JsonNullable<Boolean> destructiveHint) {
            Utils.checkNotNull(destructiveHint, "destructiveHint");
            this.destructiveHint = destructiveHint;
            return this;
        }


        /**
         * True if repeated calls with the same arguments have no additional effect.
         * Always present; as with `readOnlyHint`, `false` means "not declared
         * idempotent". Only meaningful when `readOnlyHint` is `false`.
         */
        public Builder idempotentHint(boolean idempotentHint) {
            Utils.checkNotNull(idempotentHint, "idempotentHint");
            this.idempotentHint = Optional.ofNullable(idempotentHint);
            return this;
        }

        /**
         * True if repeated calls with the same arguments have no additional effect.
         * Always present; as with `readOnlyHint`, `false` means "not declared
         * idempotent". Only meaningful when `readOnlyHint` is `false`.
         */
        public Builder idempotentHint(Optional<Boolean> idempotentHint) {
            Utils.checkNotNull(idempotentHint, "idempotentHint");
            this.idempotentHint = idempotentHint;
            return this;
        }


        /**
         * True if the tool interacts with an open world of external entities. **Absent
         * means the server did not declare it**, for which the MCP default is `true`.
         */
        public Builder openWorldHint(boolean openWorldHint) {
            Utils.checkNotNull(openWorldHint, "openWorldHint");
            this.openWorldHint = JsonNullable.of(openWorldHint);
            return this;
        }

        /**
         * True if the tool interacts with an open world of external entities. **Absent
         * means the server did not declare it**, for which the MCP default is `true`.
         */
        public Builder openWorldHint(JsonNullable<Boolean> openWorldHint) {
            Utils.checkNotNull(openWorldHint, "openWorldHint");
            this.openWorldHint = openWorldHint;
            return this;
        }

        public ToolAnnotations build() {

            return new ToolAnnotations(
                title, readOnlyHint, destructiveHint,
                idempotentHint, openWorldHint);
        }

    }
}
