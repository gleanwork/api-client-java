# DatasourcesType

The types of datasource for which to run the report/policy.

## Example Usage

```java
import com.glean.api_client.glean_api_client.models.components.DatasourcesType;

DatasourcesType value = DatasourcesType.ALL;

// Open enum: use .of() to create instances from custom string values
DatasourcesType custom = DatasourcesType.of("custom_value");
```


## Values

| Name     | Value    |
| -------- | -------- |
| `ALL`    | ALL      |
| `CUSTOM` | CUSTOM   |